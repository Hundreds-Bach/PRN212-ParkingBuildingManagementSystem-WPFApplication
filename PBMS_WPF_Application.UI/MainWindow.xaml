<Window x:Class="PBMS_WPF_Application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PBMS_WPF_Application"
        mc:Ignorable="d"
        Title="PBMS - Đăng Nhập / Đăng Ký" 
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <!-- ==================== FORM ĐĂNG NHẬP ==================== -->
        <StackPanel x:Name="cardLogin" Visibility="Visible" VerticalAlignment="Center">
            <TextBlock Text="ĐĂNG NHẬP" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Cảnh báo lỗi đăng nhập -->
            <Border x:Name="borderLoginWarning" BorderBrush="Red" BorderThickness="1" Background="#FEEFB3" CornerRadius="3" Padding="10" Margin="0,0,0,15" Visibility="Collapsed">
                <TextBlock x:Name="lblLoginWarning" Text="Lỗi" Foreground="Red" TextWrapping="Wrap"/>
            </Border>

            <!-- Nhập liệu -->
            <TextBlock Text="Số điện thoại:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLoginPhone" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Text="Mật khẩu:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="pbLoginPassword" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

            <Button x:Name="btnLogin" Content="Đăng Nhập" Height="35" FontWeight="Bold" Click="BtnLogin_Click" Margin="0,0,0,15"/>

            <Button x:Name="btnGoToRegister" Content="Chưa có tài khoản? Đăng ký ngay" Background="Transparent" BorderThickness="0" Foreground="Blue" Cursor="Hand" Click="BtnGoToRegister_Click"/>
        </StackPanel>

        <!-- ==================== FORM ĐĂNG KÝ ==================== -->
        <StackPanel x:Name="cardRegister" Visibility="Collapsed" VerticalAlignment="Center">
            <TextBlock Text="ĐĂNG KÝ TÀI KHOẢN" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Cảnh báo lỗi đăng ký -->
            <Border x:Name="borderRegWarning" BorderBrush="Orange" BorderThickness="1" Background="#FEEFB3" CornerRadius="3" Padding="10" Margin="0,0,0,15" Visibility="Collapsed">
                <TextBlock x:Name="lblRegWarning" Text="Lỗi" Foreground="DarkOrange" TextWrapping="Wrap"/>
            </Border>

            <!-- Nhập liệu -->
            <TextBlock Text="Tên tài khoản (Username):" Margin="0,0,0,5"/>
            <TextBox x:Name="txtRegUsername" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="Email:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtRegEmail" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="Số điện thoại:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtRegPhone" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="Mật khẩu:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="pbRegPassword" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

            <Button x:Name="btnRegister" Content="Đăng Ký" Height="35" FontWeight="Bold" Click="BtnRegister_Click" Margin="0,0,0,15"/>

            <Button x:Name="btnGoToLogin" Content="Đã có tài khoản? Đăng nhập tại đây" Background="Transparent" BorderThickness="0" Foreground="Blue" Cursor="Hand" Click="BtnGoToLogin_Click"/>
        </StackPanel>
    </Grid>
</Window>
